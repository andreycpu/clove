{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "prompt",
            "if": "Bash(git push:*)",
            "prompt": "STOP. Before this git push proceeds, you MUST run /security-review on the current diff. Review all changes for: hardcoded secrets, API keys, tokens, command injection, path traversal, unsafe deserialization, insecure file permissions, and data exposure. If any HIGH or MEDIUM severity issues are found, BLOCK the push and explain what needs to be fixed. Only allow the push if the review finds no issues.",
            "statusMessage": "Running security review before push..."
          }
        ]
      }
    ]
  }
}
